<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />


    <style name="main_bar_text_style">
        <item name="android:textColor">@color/ffffff</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textSize">18sp</item>
    </style>

    <style name="channel_collapsedTitleTextAppearance">
        <item name="android:textColor">@color/ffffff</item>
        <item name="android:textSize">@dimen/text_size_32</item>
    </style>

    <style name="channel_expandedTitleTextAppearance">
        <item name="android:textColor">@color/comm_transparent</item>
        <item name="android:textSize">@dimen/text_size_32</item>
    </style>

    <style name="tablayoutIcon" parent="TextAppearance.Design.Tab">
        <item name="textAllCaps">false</item>
        <item name="android:gravity">center_vertical</item>
        <item name="android:layout_width">match_parent</item>
        <item name="android:textSize">@dimen/text_size_24</item>
    </style>

    <style name="splite_style">
        <item name="android:layout_width">match_parent</item>
        <item name="android:background">@color/eaeaea</item>
        <item name="android:layout_height">1px</item>
    </style>

    <declare-styleable name="PullHeaderLayout">
        <attr name="phl_header_height" format="dimension" />
        <attr name="phl_header_expand_height" format="dimension" />
        <attr name="phl_header_shrink_height" format="dimension" />
        <attr name="phl_header" format="reference" />
        <attr name="phl_content" format="reference" />
        <attr name="phl_action" format="reference" />
    </declare-styleable>
</resources>
